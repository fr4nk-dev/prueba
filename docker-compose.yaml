version: "3.7"

services:
  reverseproxy:
    build:
      context: ./Nginx
      dockerfile: Dockerfile
    ports:
      - "8080:443"
    restart: always
    depends_on:
      - api
  api:
    build:
      context: ./DemoApi
      dockerfile: Dockerfile
    expose:
      - "5000"
    restart: always
